# Issues

## Open Issues

1. **NPM missing:** Tailwind compiled via CDN for now. Need local node to compile full build.

## Closed Issues
- Reverb `channels.php` missing due to `--no-interaction` abort on prompt. Fixed by adding keys directly to `.env`.
